import stratagemsState from '../App';
import { useRecoilState } from "recoil";
import { stratagemsState } from '../App';

export default function StratagemsList() {

    const list = useRecoilState();

    return (
        <div>

        </div>
    );
};